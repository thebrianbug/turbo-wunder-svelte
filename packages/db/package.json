{
  "name": "db",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push --skip-generate",
    "seed": "prisma db seed"
  },
  "dependencies": {
    "@prisma/client": "^4.6.1"
  },
  "devDependencies": {
    "prisma": "^4.6.1",
    "ts-node": "^10.9.1"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  }
}
